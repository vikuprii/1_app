let initialState = {
    friends: [
      { id: 1, name: 'Kiril' },
      { id: 2, name: 'Max' },
      { id: 3, name: 'Boris' }]
  }

const sidebarReducer = (state=initialState, action) => {

    return state;
}

export default sidebarReducer;